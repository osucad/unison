# dds

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build dds` to build the library.

## Running unit tests

Run `nx test dds` to execute the unit tests via [Vitest](https://vitest.dev/).
